"""space — delete/downgrade/upgrade PLANNERS (emit plans, no HTTP).

The space-reclamation brain: rank + decide what to delete/downgrade/upgrade and
the cross-service coordinator's pure ranking math. Highest-risk; migrated last,
one planner per PR (MIGRATION.md Step 7).
"""
